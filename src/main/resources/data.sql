CREATE TABLE IF NOT EXISTS PERSON;

create table PERSON (
	id INT auto_increment,
	firstname VARCHAR(50),
	lastname VARCHAR(50),
	email VARCHAR(50),
	gender VARCHAR(50),
	address VARCHAR(50),
	country VARCHAR(50),
	zipcode VARCHAR(50),
	timezone VARCHAR(50)
);
